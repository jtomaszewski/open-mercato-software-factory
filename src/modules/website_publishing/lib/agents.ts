/** The orchestrator file agent (`agents/developer`) that changes the website checkout inside the sidecar's sandbox. */
export const DEVELOPER_AGENT_ID = 'website_publishing.developer'

/** The orchestrator file agent (`agents/researcher`) that reads a customer's website with `web_fetch` (SPEC-006). */
export const RESEARCHER_AGENT_ID = 'website_publishing.researcher'
